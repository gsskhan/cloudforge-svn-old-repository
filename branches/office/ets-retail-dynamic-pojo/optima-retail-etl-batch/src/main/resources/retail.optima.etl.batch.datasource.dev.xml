<?xml version="1.0" encoding="UTF-8"?>

<beans	xmlns="http://www.springframework.org/schema/beans"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xmlns:p="http://www.springframework.org/schema/p"
		xmlns:context="http://www.springframework.org/schema/context"
		xsi:schemaLocation="
				http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
				http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd">

 	<bean	id="optimaDbPool"
			class="oracle.ucp.jdbc.PoolDataSourceFactory"
			factory-method="getPoolDataSource"
			lazy-init="true"
			p:connectionFactoryClassName="${optima.db.dev.driverClassName}"
			p:SQLForValidateConnection="select 1 from DUAL"			
			p:URL="${optima.db.dev.url}"
			p:user="${optima.db.dev.user}"
			p:password="${optima.db.dev.pass}"
			p:connectionPoolName="${optima.db.dev.pool.name}"
			p:minPoolSize="${optima.db.dev.pool.min}"
			p:maxPoolSize="${optima.db.dev.pool.max}"
	/>
	
	<bean	id="genesisEmulatorDbPool"
			class="oracle.ucp.jdbc.PoolDataSourceFactory"
			factory-method="getPoolDataSource"
			lazy-init="true"
			p:connectionFactoryClassName="${optima.db.dev.driverClassName}"
			p:SQLForValidateConnection="select 1 from DUAL"			
			p:URL="${genesisEmulator.db.dev.url}"
			p:user="${genesisEmulator.db.dev.user}"
			p:password="${genesisEmulator.db.dev.pass}"
			p:connectionPoolName="${genesisEmulator.db.dev.pool.name}"
			p:minPoolSize="${genesisEmulator.db.dev.pool.min}"
			p:maxPoolSize="${genesisEmulator.db.dev.pool.max}"
	/>	
 
    <bean	id="jdbcTemplate"
    		class="org.springframework.jdbc.core.JdbcTemplate"
    		p:dataSource-ref="optimaDbPool"
    />	
    <bean	id="batchAuditLogger" 
			class="com.citi.retail.optima.etl.controller.audit.dao.BatchAuditLoggerImpl">
			<property name="dataSource" ref="optimaDbPool" />
			</bean>
			
	 <bean	id="connectionManager" class="com.citi.optima.batch.job.jdbc.ConnectionManager" >
	 	<property name="dbDriverName" value="${optima.db.dev.driverClassName}"></property>
	 	<property name="dbUri" value="${optima.db.dev.url}" ></property>
	 	<property name="dbUser" value="${optima.db.dev.user}" ></property>
	 	<property name="dbPwd" value="${optima.db.dev.pass}" ></property>
	 </bean> 		

   
    <bean	id="queryExecutor" 
			class="com.citi.optima.batch.job.jdbc.ETLQueryExecutor">
			<property name="connectionManager" ref="connectionManager" />
			<property name="jdbcTemplate" ref="jdbcTemplate" />
			</bean>
</beans>
