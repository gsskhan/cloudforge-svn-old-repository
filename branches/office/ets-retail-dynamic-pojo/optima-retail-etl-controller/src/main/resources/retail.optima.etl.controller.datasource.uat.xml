<?xml version="1.0" encoding="UTF-8"?>

<beans	xmlns="http://www.springframework.org/schema/beans"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xmlns:p="http://www.springframework.org/schema/p"
		xmlns:context="http://www.springframework.org/schema/context"
		xsi:schemaLocation="
				http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
				http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd">

 	<bean	id="optimaLabPool"
				class="oracle.ucp.jdbc.PoolDataSourceFactory"
			factory-method="getPoolDataSource"
			lazy-init="true"
			p:connectionFactoryClassName="${optima.db.uat.driverClassName}"
			p:SQLForValidateConnection="select 1 from DUAL"			
			p:URL="${optima.db.uat.url}"
			p:user="${optima.db.uat.user}"
			p:password="${optima.db.uat.pass}"
			p:connectionPoolName="${optima.db.uat.pool.name}"
			p:minPoolSize="${optima.db.uat.pool.min}"
			p:maxPoolSize="${optima.db.uat.pool.max}"
	/>
    <bean	id="jdbcTemplate"
    		class="org.springframework.jdbc.core.JdbcTemplate"
    		p:dataSource-ref="optimaLabPool"
    />	
</beans>